# 自由报表系统设计说明书

**版本：** v0.2.0
**日期：** 2026-07-27
**状态：** 正式发布（Spring Boot 架构版）

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术架构](#2-技术架构)
3. [数据库设计](#3-数据库设计)
4. [后端架构](#4-后端架构)
5. [前端架构](#5-前端架构)
6. [核心业务流程](#6-核心业务流程)
7. [权限与安全](#7-权限与安全)
8. [性能优化](#8-性能优化)
9. [API 接口清单](#9-api-接口清单)
10. [部署与运维](#10-部署与运维)

---

## 1. 系统概述

### 1.1 系统定位

自由报表（FreeReport / 随手报 ReportNow）是一套面向企业总部-部门-分公司三级组织架构的报表填报与审批系统。核心能力包括：

- **模板设计**：支持文本、数字、日期、下拉选择、多行文本及二维交叉表字段
- **模板审批**：部门创建的模板须经数智化转型办公室审批发布后方可下发
- **周期下发**：按模板向指定分公司下发填报任务，支持一次性下发
- **三级审批**：经办人填报 → 复核人审核 → 审批人终审 → 发起部门签收
- **强制收回**：发起部门可强制收回已下发的任务
- **智能汇总**：多机构数据横向对比，SQL 下沉聚合，支持 Excel 导出
- **部门隔离**：严格的部门级数据访问控制

### 1.2 组织模型

```
总部 (headquarter)
├── 业务综合管理部 (department)
├── 办公室 (department)
├── 风险管理部 (department)
├── 数智化转型办公室 (department)   -- 模板审批专属部门
├── 北京分公司 (branch)
├── 上海分公司 (branch)
└── 广州分公司 (branch)
```

### 1.3 角色矩阵

| 角色 | 填报 | 审批填报 | 模板管理 | 模板审批 | 下发 | 签收 | 汇总查看 | 机构管理 |
|------|:----:|:--------:|:--------:|:--------:|:----:|:----:|:--------:|:--------:|
| 超级管理员 | - | - | 只读 | - | - | - | 只读 | ✓ |
| 部门报表管理员 | - | - | ✓ | 提交 | ✓ | ✓ | ✓ | - |
| 数智化转型办公室 | - | - | 只读全部 | ✓ | - | - | - | - |
| 分公司管理员 | ✓ | - | - | - | - | - | - | - |
| 经办人 | ✓ | - | - | - | - | - | - | - |
| 复核人 | - | ✓ | - | - | - | - | - | - |
| 审批人 | - | ✓ | - | - | - | - | - | - |

> 数智化转型办公室（`digital_admin`）不归属任何业务部门的管理链路，专职模板审批；可只读查看所有部门的模板。

---

## 2. 技术架构

### 2.1 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React | 19.x |
| 构建工具 | Vite | 6.x |
| 样式方案 | Tailwind CSS | 4.x（@theme inline 语义色） |
| 路由 | React Router DOM | 7.x |
| 数据缓存 | SWR | 2.x |
| 图标 | @phosphor-icons/react | 2.1 |
| Excel 导入导出 | xlsx | 0.18 |
| 后端框架 | Spring Boot | 3.3.2 |
| 语言 | Java | 17 |
| ORM | MyBatis | 3.0.3（注解 + XML 混合） |
| 数据库 | MySQL | 8.0（InnoDB / utf8mb4） |
| 认证 | JWT（jjwt） | 24h 有效期 |
| 密码哈希 | BCrypt | Spring Security Crypto |

### 2.2 工程结构

```
free-report/
├── src/                          # 前端（React + Vite）
│   ├── pages/                    # 页面（React.lazy 懒加载）
│   ├── components/               # 通用组件
│   │   └── report/               # ReportFill 拆分出的填报子组件
│   ├── services/api.ts           # API 统一封装 + SWR hooks
│   ├── utils/                    # access/templateLifecycle/submissionWorkflow 等
│   └── types.ts                  # 全局 TypeScript 类型
├── server-springboot/            # 后端（Spring Boot）
│   └── src/main/java/com/freereport/
│       ├── controller/           # REST 入口（10 个 Controller）
│       ├── service/              # 业务逻辑（9 个 Service）
│       ├── mapper/               # MyBatis 接口（8 个 Mapper）
│       ├── entity/               # 数据库实体（12 个 POJO）
│       ├── dto/                  # 请求/响应对象
│       ├── security/             # JWT 过滤器、Token 提供器、SecurityUtils
│       ├── exception/            # DomainException + 全局异常处理
│       └── config/               # Security/Web/Filter 配置
│   └── src/main/resources/mapper/  # MyBatis XML（8 个）
├── sql/                          # 迁移脚本 001 ~ 008
├── docs/schema-current.sql       # 当前完整结构参考（合并全部迁移）
└── start.sh                      # 一键启动脚本
```

---

## 3. 数据库设计

### 3.1 表清单（12 张）

| 表 | 说明 |
|----|------|
| companies | 组织机构（总部 / 部门 / 分公司，自引用树） |
| users | 用户（角色枚举含 digital_admin） |
| report_templates | 报表模板（owner_department_id 归属部门） |
| report_template_fields | 模板字段（summary / detail / matrix 三类） |
| report_assignments | 下发任务（issuer_department_id 发起部门） |
| assignment_recalls | 强制收回审计 |
| report_submissions | 填报提交（assignment_id + version 多版本） |
| report_submission_data | 填报明细（EAV + row_index，含 numeric_value 冗余列） |
| approval_records | 分公司内部三级审批记录 |
| submission_receipts | 发起部门签收记录 |
| report_aggregations | 汇总结果快照（JSON） |
| template_approvals | 模板审批记录（数智化转型办公室） |
| schema_migrations | 迁移执行记录 |

> 完整字段级 DDL 见 [schema-current.sql](./schema-current.sql)，每处变更标注来源迁移文件。

### 3.2 关键设计

**report_submission_data（EAV + 数值冗余）**

- `value` TEXT 存储原始填报值，`row_index=0` 为汇总区、`>0` 为明细行 / 矩阵单元格
- `numeric_value DECIMAL(18,4)` 冗余列（008 新增）：写入时按正则 `^-?[0-9]+\.?[0-9]*$` 同步填充，非数值置 NULL；聚合查询直接 `SUM(numeric_value)`，避免逐行 CAST，并可利用 `idx_sd_numeric (field_id, numeric_value)` 索引

**report_templates.status 生命周期**

```
draft → pending_approval → published ↔ archived
  ↑___________驳回____________|
```

**report_submissions.status 状态机**

```
draft → pending_review → pending_approval → pending_receipt → received
  ↑________驳回 rejected________|        ↑____退回 returned____|
```

### 3.3 迁移文件

| 文件 | 内容 |
|------|------|
| 001_schema.sql | 基础建表 |
| 002_seed.sql | 种子数据（9 机构、15 用户、2 模板，含数转办账号） |
| 003_fix_vehicle_detail_fields.sql | 字段分类修正 |
| 004_department_reporting.sql | 部门层级 + 签收流程 |
| 005_department_admin_backfill.sql | 历史角色迁移 |
| 006_performance_indexes.sql | 性能索引补充 |
| 007_recall_and_onetime.sql | 强制收回 + 一次性下发 |
| 008_template_approval.sql | 模板审批流 + digital_admin 角色 + numeric_value 冗余列 |

---

## 4. 后端架构

### 4.1 分层职责

| 层 | 职责 | 代表类 |
|----|------|--------|
| Controller | 参数校验、鉴权调用、响应组装 | TemplateController 等 10 个 |
| Service | 业务规则、事务边界（@Transactional） | TemplateService、SubmissionService |
| Mapper | MyBatis 数据访问（接口 + XML） | TemplateMapper + TemplateMapper.xml |
| Entity / DTO | 数据库映射 / 接口契约（SnakeCase JSON） | ReportTemplate、CreateSubmissionRequest |
| security | JWT 认证与授权判定 | JwtAuthFilter、SecurityUtils |
| exception | 领域异常 + 全局错误响应 | DomainException、GlobalExceptionHandler |

### 4.2 认证与授权

- **JwtAuthFilter**：解析 Bearer Token，将 `AuthUser` 写入 SecurityContext
- **SecurityUtils**：通过 `RequestContextHolder` 获取当前请求用户（非构造器注入），提供：
  - `getCurrentUser()` / `isSuperAdmin()` / `isDepartmentReportAdmin()` / `isDigitalAdmin()`
  - `requireSuperAdmin()` / `requireDigitalAdmin()` 等守卫，越权抛 403
  - `canReadTemplate()` / `canReadAssignment()` 等资源级判定（数转办可读全部模板）
- **密码**：BCrypt 哈希；Token 24h 有效期，密钥由 `JWT_SECRET` 配置

### 4.3 部门隔离策略

| 操作 | 超管 | 部门管理员 | 数转办 | 分公司用户 |
|------|:----:|:--------:|:------:|:----------:|
| 读模板 | ✓ 全部 | ✓ 本部门 | ✓ 全部 | - |
| 管理模板 | - | ✓ 本部门 | - | - |
| 审批模板 | - | 提交 | ✓ | - |
| 读任务 | ✓ 全部 | ✓ 本部门发起 + 被下发 | - | ✓ 本机构被下发 |
| 写任务（填报） | - | - | - | ✓ handler/branch_admin |
| 签收 | - | ✓ 本部门 | - | - |

### 4.4 事务与并发

- 多步写操作均在 `@Transactional` 内，失败整体回滚
- 模板编辑、提交、审批等关键路径使用行级锁（`SELECT ... FOR UPDATE`）防并发竞态
- 状态变更前置校验当前状态（如仅 `pending_approval` 可审批），天然幂等

---

## 5. 前端架构

### 5.1 路由与代码分割

React.lazy + Suspense 懒加载全部页面；受保护路由统一包裹 `ErrorBoundary`（防白屏，含错误提示与刷新按钮）。

### 5.2 数据请求与缓存

- **统一封装** `request<T>()`：注入 Authorization；401 时通过 `window.dispatchEvent(new Event('auth:unauthorized'))` 发出事件，由 App 内 `UnauthorizedHandler` 执行 SPA 内 navigate('/login')，避免硬刷新
- **SWR hooks**：templates / assignments / branches / pendingApprovals / pendingReceipts 等，统一 `revalidateOnFocus: false`，按角色条件请求（`useSWR(cond ? key : null)`）
- **分页**：模板 / 任务列表支持服务端分页（`?page=&size=`），响应 `{ data, total, page, size }`；不传分页参数时保持原数组响应兼容看板类页面

### 5.3 权限控制

`getClientAccess(user)` 返回布尔标志集驱动条件渲染：

```typescript
{
  isSuperAdmin, isDepartmentAdmin, isDigitalAdmin,
  canManageTemplates,     // 模板管理（部门管理员）
  canApproveTemplates,    // 模板审批（数转办）
  canFill, canReceive, canManageOrganizations,
}
```

模板生命周期视图由 `templateLifecycle.ts` 统一计算：`canWrite / canAssign / canSubmitApproval`（draft 可编辑可提交审批、pending_approval 锁定、published 可下发）。

### 5.4 页面清单

| 页面 | 路由 | 功能 |
|------|------|------|
| Dashboard | / | 工作台（待签收、统计、任务、使用帮助） |
| Login | /login | 登录（DEV 预设账号） |
| TemplateList | /templates | 模板列表（分页、提交审批、状态标签） |
| TemplateEditor | /templates/:id | 模板设计器 |
| TemplateApprovalPanel | （嵌入审批中心） | 模板审批面板（数转办：通过/驳回/意见） |
| AssignmentList | /assignments | 下发任务管理（含收回） |
| ReportFill | /fill/:assignmentId | 报表填报 |
| ApprovalList | /approvals | 审批中心（填报审批 / 模板审批标签页） |
| AggregationView | /aggregation | 汇总报表 |
| OrganizationManagement | /organizations | 机构与用户管理 |
| GlobalReadOnlyView | /global-view | 全局只读视图（超管） |

ReportFill 由 5 个子组件组合：`SummaryForm`（汇总区）、`DetailTable`（明细 + Excel 导入导出）、`CrossTable`（交叉表）、`ApprovalTimeline`（审批时间线）、`ReportActions`（操作按钮组）。

### 5.5 设计系统

Premium Utilitarian Minimalism，CSS 变量驱动双主题：

- **暖单色（默认）**：`--bg #F7F6F3`，近黑强调 `--accent #111111`
- **海蓝（`data-theme="blue"`）**：ThemeSwitcher 一键切换
- 语义色经 Tailwind v4 `@theme inline` 映射为 `text-ink / text-mute / bg-canvas` 等工具类
- 统一面板 + 发丝分隔线；数字 `tabular-nums`；中英文混排衬线大标题

---

## 6. 核心业务流程

### 6.1 模板审批流程（数智化转型办公室）

```
部门报表管理员创建模板（draft）
    │
    ▼
模板列表点击「提交审批」── 创建 template_approvals(pending) 记录
    │
    ▼
pending_approval（锁定编辑、禁止下发）
    │
    ├── 数转办「通过」──► published（可下发），审批记录置 approved
    │
    └── 数转办「驳回」──► draft（可修改重提），审批记录置 rejected
```

**约束**：

- 仅 `digital_admin` 可执行通过 / 驳回（`requireDigitalAdmin`），可附审批意见
- 仅 `draft` 可提交审批；`pending_approval` 状态模板编辑、下发均被服务端拦截
- 审批接口校验模板当前状态，重复审批无效

### 6.2 三级审批 + 签收流程

```
经办人填报并提交
    │
    ▼
pending_review → 复核人通过 → pending_approval → 审批人通过 → pending_receipt
                                                                    │
                                                              发起部门签收
                                                                    │
                                                        ┌───────────┴───────────┐
                                                        ▼                       ▼
                                                    received                returned（退回）
```

任意审批环节可驳回（`rejected`），经办人修改后重新提交，流程从头开始。

### 6.3 强制收回流程

```
发起部门管理员 → 选择未汇总任务 → 填写收回原因 → 确认收回
    │
    ▼
任务状态 → recalled
    ├── 写入 assignment_recalls 审计表
    └── 取消相关 pending 审批记录（标记 rejected）
```

**约束**：仅发起部门管理员可收回；已汇总（aggregated）任务不可收回；不可重复收回；原因必填。

### 6.4 一次性下发

下发时勾选「一次性下发」→ `period_label` 追加唯一时间戳后缀（如 `2026年07月 #1784862420000`），`is_one_time=1`，绕过 `uq_assignment_period` 唯一约束。适用于补充调查、临时加报。

### 6.5 汇总逻辑

- 按模板 + 周期筛选下发任务，取每个任务的最新版本已批准填报（状态 ∈ pending_receipt / received）
- **SQL 下沉聚合**：`SUM(d.numeric_value)`、`COUNT(DISTINCT s.id)` 在数据库层完成，替代 Java 内存 Stream 聚合
- 明细行合并展示；仅已审批通过的填报计入统计
- 支持 Excel 导出（机构对比表 + 明细表 + 进度表）

---

## 7. 权限与安全

### 7.1 认证机制

- **密码存储**：BCrypt 哈希，不可逆
- **Token**：JWT，24h 有效期，生产环境强制配置 `JWT_SECRET`
- **传输**：Bearer token via Authorization header

### 7.2 请求安全

- **CORS 白名单**：`CORS_ORIGINS` 配置允许的源
- **SQL 注入防护**：MyBatis 参数化查询
- **错误屏蔽**：GlobalExceptionHandler 统一出口，500 返回通用文案，不泄露堆栈
- **参数校验**：DTO 层 jakarta.validation（@NotNull 等），400 返回字段级错误

### 7.3 并发安全

- **悲观锁**：模板 / 任务 / 填报写操作 `SELECT ... FOR UPDATE`
- **事务**：所有多步写操作 `@Transactional`，失败自动回滚
- **幂等性**：模板启停、审批、状态变更前置检查当前状态

### 7.4 数据隔离

通过 `owner_department_id` / `issuer_department_id` / `assigned_to_company_id` 三字段 + SecurityUtils 资源级判定实现：

- 部门管理员仅访问本部门模板和本部门发起的下发任务
- 数智化转型办公室可读全部模板，仅可执行模板审批
- 分公司用户仅访问本机构被下发的任务
- 超级管理员全局只读，机构与用户管理可写

### 7.5 改进建议

- **速率限制**：登录等敏感端点建议添加防暴力破解保护
- **审计日志**：建议扩展审计表覆盖所有状态变更操作

---

## 8. 性能优化

### 8.1 数据库优化

| 优化项 | 说明 |
|--------|------|
| numeric_value 冗余列 | 聚合免逐行 CAST，`idx_sd_numeric` 索引加速 |
| SQL 下沉聚合 | SUM / COUNT 在 SQL 层完成，避免全量明细回传 |
| 列表分页 | 模板 / 任务列表 `LIMIT ? OFFSET ?`，避免全表加载 |
| 性能索引 | 006 迁移补充 submissions / approvals 复合索引 |
| 批量查询 | IN 查询 + Map 关联，消除 N+1 |
| 悲观锁 | FOR UPDATE 防并发竞态 |

### 8.2 后端优化

| 优化项 | 说明 |
|--------|------|
| HikariCP 连接池 | Spring Boot 默认，复用连接 |
| DTO SnakeCase 序列化 | Jackson @JsonNaming 统一命名策略 |
| 全局异常处理 | 统一错误响应，避免异常穿透 |

### 8.3 前端优化

| 优化项 | 说明 |
|--------|------|
| 代码分割 | React.lazy 全页面懒加载 |
| SWR 缓存 | 统一 hooks，`revalidateOnFocus: false` |
| 条件请求 | 按角色条件加载，减少不必要请求 |
| ErrorBoundary | 局部错误不扩散为整页白屏 |
| 401 事件总线 | SPA 内跳转替代 window.location 硬刷新 |

---

## 9. API 接口清单

### 9.1 认证

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/auth/login | 登录 | 公开 |
| GET | /api/auth/me | 获取当前用户 | 已认证 |

### 9.2 机构

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/companies | 获取机构树 | 已认证 |
| GET | /api/companies/branches | 获取分公司列表 | 已认证 |
| GET | /api/companies/targets | 获取下发目标 | 已认证 |
| POST | /api/companies | 创建机构 | 超级管理员 |
| PUT | /api/companies/:id/disable | 停用机构 | 超级管理员 |

### 9.3 模板

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/templates | 模板列表（可选 `?page=&size=` 分页） | 已认证 |
| POST | /api/templates | 创建模板（初始 draft） | 部门管理员 |
| GET | /api/templates/:id | 模板详情 | 已认证 |
| PUT | /api/templates/:id | 更新模板 | 部门管理员 |
| PUT | /api/templates/:id/disable | 归档模板 | 部门管理员 |
| PUT | /api/templates/:id/enable | 启用模板 | 部门管理员 |
| POST | /api/templates/:id/fields | 添加字段 | 部门管理员 |
| PUT | /api/templates/:id/fields/:fieldId/disable | 停用字段 | 部门管理员 |
| POST | /api/templates/:id/matrix-fields | 添加交叉表字段 | 部门管理员 |
| POST | /api/templates/:id/assign | 下发模板（仅 published） | 部门管理员 |
| POST | /api/templates/:id/submit-approval | 提交模板审批 | 部门管理员 |
| GET | /api/templates/pending-approvals | 待审批模板列表 | 数转办 |
| POST | /api/templates/:id/approve | 审批通过（可附意见） | 数转办 |
| POST | /api/templates/:id/reject | 审批驳回（可附意见） | 数转办 |

### 9.4 下发任务

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/assignments | 任务列表（可选 `?page=&size=` 分页） | 已认证 |
| GET | /api/assignments/:id | 任务详情 | 已认证 |
| POST | /api/assignments/:id/recall | 强制收回 | 部门管理员 |

### 9.5 填报

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/submissions | 保存草稿 / 提交填报 | 经办人 |
| POST | /api/submissions/:id/submit | 提交已有草稿 | 经办人 |
| GET | /api/submissions/:id | 填报详情 | 已认证 |
| GET | /api/submissions/by-assignment/:assignmentId | 按任务查最新填报 | 已认证 |

### 9.6 审批（分公司内部）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/approvals/pending | 待审批列表 | 已认证 |
| POST | /api/approvals/:submissionId/action | 审批操作 | 已认证 |

### 9.7 签收

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/receipts/pending | 待签收列表 | 部门管理员 |
| POST | /api/receipts/:submissionId/action | 签收 / 退回 | 部门管理员 |

### 9.8 汇总

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/aggregations/by-template/:templateId?period_label= | 按模板 + 周期查汇总 | 已认证 |
| POST | /api/aggregations/aggregate/:assignmentId | 执行汇总 | 部门管理员 |
| GET | /api/aggregations/history/:templateId | 历史汇总 | 已认证 |

### 9.9 用户管理

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/users | 用户列表 | 超级管理员 |
| PUT | /api/users/:id/organization-role | 更新用户机构与角色 | 超级管理员 |

### 9.10 其他

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/health | 健康检查 | 公开 |

---

## 10. 部署与运维

### 10.1 环境配置

后端 `server-springboot/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:freereport}?...
    username: ${MYSQL_USER:freereport}
    password: ${MYSQL_PASSWORD:freereport123}
jwt:
  secret: ${JWT_SECRET:your-jwt-secret-here}
  expiration: 86400000
cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
```

前端 `.env`（开发代理）：`VITE_API_BASE_URL=http://localhost:3001`

### 10.2 部署步骤

1. **数据库初始化**：按序执行 `sql/001_schema.sql` 至 `sql/008_template_approval.sql`
2. **安装依赖**：`npm install`
3. **后端构建**：`cd server-springboot && mvn clean package -DskipTests`
4. **前端构建**：`npm run build`
5. **启动**：`java -jar server-springboot/target/free-report-server-1.0.0.jar`
6. **一键启动（开发）**：`./start.sh`（自动检测 JDK 17 / MySQL、清理端口、启动前后端并健康检查）

### 10.3 验证

```bash
# 前端类型检查
npx tsc --noEmit

# 后端编译
cd server-springboot && mvn clean package -DskipTests
```

模板审批走查路径：`hq_admin` 创建模板（draft）→ 提交审批 → `digital_admin` 通过 / 驳回 → 通过后 `hq_admin` 下发。

### 10.4 监控

- 健康检查端点：`GET /api/health`
- 错误日志：GlobalExceptionHandler 统一捕获并记录
- 数据库迁移记录：`schema_migrations` 表

---

*本文档随系统迭代持续更新。*
